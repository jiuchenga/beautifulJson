---
// src/pages/[lang]/encrypt/number-base.astro
import ToolLayout from '@/layouts/ToolLayout.astro';
import ToolContent from '@/components/common/ToolContent.astro';
import { languages, type Lang } from '@/i18n/ui';
import { getCategories, getCommon, getTools, getToolContent } from '@/i18n/t';
import NumberBaseConverter from '@/components/tools/encrypt/NumberBaseConverter';

export function getStaticPaths() {
  return Object.keys(languages).map(lang => ({ params: { lang } }));
}

const { lang } = Astro.params;
const langTyped = lang as Lang;
const common = getCommon(langTyped);
const categories = getCategories(langTyped);
const tools = getTools(langTyped);
const toolKey = 'encrypt/number-base';
const toolData = tools[toolKey];
const catData = categories['encrypt'];
const tc = getToolContent(langTyped, 'encrypt', 'number-base');

const breadcrumbs = [
  { name: common.home, url: `/${lang}` },
  { name: catData?.name ?? 'encrypt', url: `/${lang}/category/encrypt` },
  { name: toolData?.name ?? 'number-base', url: `/${lang}/encrypt/number-base` },
];

const features = tc?.features ?? [];

const steps = tc?.steps ?? [];

const faq = tc?.faq ?? [];

const relatedTools: { title: string; href: string }[] = [

];
---

<ToolLayout
  title={`${toolData?.name ?? 'number-base'} - Free Online Tool | BeautifulJSON`}
  description={toolData?.description ?? ''}
  path="/encrypt/number-base"
  siteUrl="https://formatbeautifuljson.com"
  lang={langTyped}
  toolName={toolData?.name ?? 'number-base'}
  toolDescription={toolData?.description ?? ''}
  breadcrumbs={breadcrumbs}
>
  <NumberBaseConverter client:load lang={langTyped} title={toolData?.name ?? ''} description={toolData?.description ?? ''} />

  <ToolContent
    toolName={toolData?.name ?? 'number-base'}
    features={features}
    steps={steps}
    faq={faq}
    relatedTools={relatedTools}
    siteUrl="https://formatbeautifuljson.com"
    lang={langTyped}
  />
</ToolLayout>
